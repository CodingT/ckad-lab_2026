## Question 18 – Create Pod with Exposed Port

A web application requires a specific version of Redis to be used as a cache.

Your task:
Create a Pod named `cache` in namespace `web` with:
- Image: `redis:3.2`
- Exposed port: `6379`
- Pod must run and remain running
